# Make the engines range in package.json a hard requirement rather than a warning.
# Without this, installing under npm 11+ silently rewrites package-lock.json
# (it adds "libc" fields to optional platform deps), producing a 60-line diff
# that looks like a dependency change but is only a toolchain mismatch.
engine-strict=true
